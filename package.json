{
  "name": "facebook-ai-bot",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.6",
  "scripts": {
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "workspaces": ["client", "server", "drizzle"]
}
