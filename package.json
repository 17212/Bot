{
  "name": "facebook-ai-bot-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "lint": "npm --workspace client run lint && npm --workspace server run lint",
    "format": "npm --workspace client run format && npm --workspace server run format"
  }
}
